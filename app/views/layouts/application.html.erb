<!DOCTYPE html>
<html>
  <head>
    <title>Sanja</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body class="bg-gray-50 text-gray-900">

    <!-- Navbar -->
<nav class="bg-white border-b shadow-sm sticky top-0 z-50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16">
      <!-- Logo -->
      <div class="flex-shrink-0 flex items-center text-xl font-bold text-red-600">
        <%= link_to "SANJA", root_path %>
      </div>

      <!-- Navigation Links -->
      <div class="hidden md:flex md:items-center space-x-6">
        <%= link_to "Accueil", root_path, class: "text-gray-700 hover:text-red-600 transition" %>
        <%= link_to "Services", services_path, class: "text-gray-700 hover:text-red-600 transition" %>
        <%= link_to "Blog", blog_path, class: "text-gray-700 hover:text-red-600 transition" %>
        <%= link_to "Contact", contact_path, class: "text-gray-700 hover:text-red-600 transition" %>
      </div>

      <!-- Responsive Hamburger (facultatif pour plus tard) -->
      <!-- Tu peux ajouter un menu mobile ici -->
    </div>
  </div>
</nav>

    <!-- Page Content -->
    <main class="min-h-screen py-10">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-800 text-white py-6 mt-10">
      <div class="max-w-7xl mx-auto px-4 text-center">
        &copy; <%= Time.current.year %> Sanja. Tous droits réservés.
      </div>
    </footer>

  </body>
</html>